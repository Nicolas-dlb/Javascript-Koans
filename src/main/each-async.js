export const eachAsync = (list, callback) => {
  //   list.map(async (value) => {
  //     const a = await callback(Number(value))
  //   })
  //   return list.map(async (value) => (value = await callback(value)))
}
